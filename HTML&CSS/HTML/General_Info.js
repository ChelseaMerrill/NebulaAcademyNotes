//tags: keywords. start tag -> end tag. some tags are self-closing
    //(<h1></h1>) :
        //<h1> = starting tag (My First Heading)
        //</h1> = closing tag (entire line is an HTML ELEMENT)
    //<input / > = self-closing/unclosed tag

//Attributes : provides additional info about HTML elements
    //always specified in start tag
    
    //Class Attriutes: specifies one or more classnames for an attriute

    //ID Attributue: specifies the unique ID for an HTML element.  Used more specifically
     